apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc-sentinel
  namespace: flux-system
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
